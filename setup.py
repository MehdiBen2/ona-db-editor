from setuptools import setup

setup(
    name="DatabaseManager",
    version="1.0",
    description="A SQLite Database Manager",
    author="Your Name",
    packages=[""],
    install_requires=[
        'pandas',
        'tkinter',
        'sqlite3',
    ],
) 